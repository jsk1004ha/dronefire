{
  "description": "C0 + singles + simultaneous pairs + ordered sequential pairs. Generated by firelab.reactive.default_reactive_conditions().",
  "condition_count": 36,
  "generation": "python -c \"import json; from firelab.reactive import default_reactive_conditions; print(json.dumps(default_reactive_conditions(), ensure_ascii=False, indent=2))\""
}
